# Distributed MiniSQL 功能报告

**项目名称**: distributed-minisql  
**版本**: 0.1.0-SNAPSHOT  
**编程语言**: Java 17  
**项目类型**: Maven 项目 + Docker 容器�? 
**源代码文�?*: 103+ Java �?

---

## 📋 项目概述

**Distributed MiniSQL** 是一�?*分布式关系型数据库原型系�?*，采�?单进程模拟分布式集群"的方式。在一�?JVM 内同时运行：
- **Coordinator** - 协调�?查询规划�?
- **MetaStore** - 元数据存�?
- **3�?DataNode** - 数据节点

用于演示和学习分布式数据库的核心概念，包括分片、副本、故障转移、负载均衡和分布式查询�?

---

## 🏗�?系统架构

### 核心四层架构

```
┌─────────────────────────────────────�?
�?     SQL解析�?(Parser)             �?
�? ├─ 词法分析 (Lexer)               �?
�? ├─ 语法分析 (Parser)              �?
�? └─ 语义分析 (Semantic)            �?
├─────────────────────────────────────�?
�?  查询优化�?(Logical Planner)      �?
�? ├─ 谓词下推 (PredicatePushdown)   �?
�? ├─ 列剪�?(ColumnPruning)         �?
�? ├─ 常数折叠 (ConstantFolding)     �?
�? └─ JOIN重排�?(JoinReordering)   �?
├─────────────────────────────────────�?
�? 物理执行�?(Physical Executor)     �?
�? ├─ 物理计划构建                    �?
�? ├─ 远程执行请求生成                �?
�? └─ 分片路由与执�?                 �?
├─────────────────────────────────────�?
�?   数据节点�?(DataNode Cluster)    �?
�? ├─ DataNode 1 (Master)            �?
�? ├─ DataNode 2 (Replica)           �?
�? └─ DataNode 3 (Replica)           �?
└─────────────────────────────────────�?
```

---

## 📦 模块详解

### 1. **SQL解析�?(Parser Module)** - 40+ �?

#### 词法分析 (Lexer)
- **Lexer.java** - 词法分析器，将SQL文本分解为Token�?
- **Token.java** - Token数据结构
- **tokenType.java** - Token类型枚举定义

#### 语法分析 (Parser)
| �?| 功能 |
|----|------|
| `Parser.java` | 主解析器，递归下降算法实现SQL语法识别 |
| `SelectStatement.java` | SELECT语句AST节点 |
| `InsertStatement.java` | INSERT语句AST节点 |
| `UpdateStatement.java` | UPDATE语句AST节点 |
| `DeleteStatement.java` | DELETE语句AST节点 |
| `CreateTableStatement.java` | CREATE TABLE语句AST节点 |
| `AlterTableStatement.java` | ALTER TABLE语句和操�?|
| `DropTableStatement.java` | DROP TABLE语句AST节点 |

#### 表达式支�?
| �?| 功能 |
|----|------|
| `BinaryExpression.java` | 二元运算表达式（+, -, *, /, =, !=等） |
| `FunctionCallExpression.java` | 函数调用表达式（COUNT, SUM, AVG等） |
| `AggregateExpression.java` | 聚合表达�?|
| `ColumnExpression.java` | 列表达式 |
| `LiteralExpression.java` | 字面量表达式 |
| `IdentifierExpression.java` | 标识符表达式 |
| `SubqueryExpression.java` | 子查询表达式 |

#### 高级SQL构�?
| �?| 功能 |
|----|------|
| `JoinClause.java` | JOIN子句和连接类�?|
| `GroupByClause.java` | GROUP BY子句 |
| `HavingClause.java` | HAVING子句 |
| `OrderByClause.java` | ORDER BY子句和排序项 |
| `LimitClause.java` | LIMIT子句 |
| `Condition.java` | WHERE条件 |

#### 表定�?
| �?| 功能 |
|----|------|
| `ColumnDefinition.java` | 列定义（名称、类型、约束） |
| `DataType.java` | SQL数据类型枚举 |
| `Constraint.java` | 约束定义 |
| `ConstraintType.java` | 约束类型 |
| `PrimaryKey.java` | 主键定义 |
| `ForeignKey.java` | 外键定义 |
| `Index.java` | 索引定义 |

#### 语义分析
- **semantic/** 目录包含语义检查和类型推断

#### 工具�?
- **ASTNode.java** - 抽象语法树节点基�?
- **ASTPrinter.java** - AST打印和调试工�?
- **TableReference.java** - 表引�?

---

### 2. **查询优化�?(Planner Module)** - 7 �?

#### 逻辑计划
| �?| 功能 |
|----|------|
| `LogicalPlan.java` | 逻辑执行计划表示 |
| `LogicalPlanBuilder.java` | 从AST构建逻辑计划 |
| `LogicalPlanPrinter.java` | 逻辑计划打印调试 |

#### 优化规则
| �?| 优化策略 |
|----|---------|
| `PredicatePushdown.java` | **谓词下推** - 将WHERE条件尽早下推到数据源 |
| `ColumnPruning.java` | **列剪�?* - 只选择需要的列，减少数据传输 |
| `ConstantFolding.java` | **常数折叠** - 编译期计算常数表达式 |
| `JoinReordering.java` | **JOIN重排�?* - 优化多表JOIN的执行顺�?|

---

### 3. **物理执行�?(Physical Module)** - 15+ �?

#### 物理计划
| �?| 功能 |
|----|------|
| `PhysicalPlan.java` | 物理执行计划表示 |
| `PhysicalPlanBuilder.java` | 从逻辑计划构建物理计划 |
| `PhysicalPlanPrinter.java` | 物理计划打印调试 |

#### 分布式执�?
| �?| 功能 |
|----|------|
| `RemoteExecutionRequest.java` | 远程执行请求 |
| `RemoteExecutionRequestBuilder.java` | 构建远程执行请求 |
| `RemoteStatement.java` | 远程SQL语句 |
| `RemoteStatementGenerator.java` | 生成发送到DataNode的SQL |

#### 集群元数�?
| �?| 功能 |
|----|------|
| `ClusterMetadata.java` | 集群整体元数�?|
| `DataNodeMetadata.java` | 数据节点元数�?|
| `TableMetadata.java` | 表元数据 |
| `ShardMetadata.java` | 分片元数�?|
| `DatabaseType.java` | 数据库类型枚�?|

---

### 4. **主要�?SQL 支持**

```sql
-- DDL (数据定义语言)
CREATE TABLE table_name (...) 
  SHARD BY HASH(column) SHARDS n REPLICAS r;
ALTER TABLE table_name ADD/DROP/MODIFY COLUMN ...;
DROP TABLE table_name;

-- DML (数据操作语言)
INSERT INTO table_name VALUES (...);
DELETE FROM table_name WHERE condition;
UPDATE table_name SET column = value WHERE condition;

-- DQL (数据查询语言)
SELECT [DISTINCT] columns FROM table WHERE condition
  [JOIN ...] [GROUP BY ...] [HAVING ...] [ORDER BY ...] [LIMIT n];

-- 分布式特�?
SHOW SHARDS;
SHOW NODES;
SHOW CLUSTER;
FAIL NODE dn;        -- 故障模拟
RECOVER NODE dn;     -- 节点恢复
```

---

## 🎯 分布式特�?

### 1. **分片管理** (Hash-based Sharding)
- 基于哈希函数分片：`SHARD BY HASH(column) SHARDS n`
- 支持可配置的分片数量
- 分片键字段自动路�?

### 2. **副本机制** (1 Master + 2+ Replicas)
- 每个分片配置副本数：`REPLICAS r`
- 主副本处理写�?
- 从副本参与读负载均衡

### 3. **故障转移** (Automatic Failover)
- 主副本故障自动切换到健康从副�?
- 更新集群拓扑
- 路由版本自动递增

### 4. **恢复补齐** (Recovery & Catching Up)
- 故障节点恢复后自动从健康副本同步缺失数据
- 支持增量和全量恢�?

### 5. **分布式查�?*
- **分片键条�?* �?分片裁剪（只查询相关分片�?
- **非分片键条件** �?广播查询（查询所有分片）
- **JOIN** �?支持分片表和非分片表的等值内连接

### 6. **负载均衡**
- 普通读请求在健康副本间轮询分配
- 最大化副本利用�?

### 7. **持久�?*
- 退出后保存快照�?`data/minisql-state.bin`
- 重启可恢复表结构、数据和集群状�?
- 支持自定义数据文件位�?

---

## 📊 数据流示�?

### 单分片查�?
```
Client Query
    �?
Parser (词法+语法+语义分析)
    �?
LogicalPlan (逻辑计划)
    �?
Optimizer (谓词下推、列剪裁�?
    �?
PhysicalPlan (物理计划)
    �?
ShardRouting (路由到DataNode 0)
    �?
RemoteSQL生成 (发送SQL到DataNode)
    �?
DataNode执行
    �?
结果返回 �?合并 �?返回客户�?
```

### 多分片查�?
```
LogicalPlan
    �?
分片裁剪 (确定需要查询的分片)
    �?
广播查询 (向多个DataNode发�?
    �?
并行执行 (每个DataNode独立执行)
    �?
结果聚合 (UNION/GROUP BY/ORDER BY)
    �?
返回最终结�?
```

---

## 🧪 测试

- **acceptance.sql** - 完整验收测试套件
- **test-cases.md** - 测试用例说明文档
- 支持本地运行和Docker运行测试

---

## 🚀 运行方式

### 本地编译运行
```powershell
mvn compile
java -cp target\classes minisql.app.App
```

### Docker 运行
```powershell
docker build -t distributed-minisql:local .
docker run --rm -it -v minisql-data:/app/data distributed-minisql:local
```

### 运行完整测试
```powershell
mvn compile
Get-Content tests\acceptance.sql | java "-Dminisql.data=target\acceptance-state.bin" -cp target\classes minisql.app.App
```

---

## 📈 性能特�?

| 特�?| 说明 |
|------|------|
| **查询耗时统计** | 每条SQL输出执行时间 |
| **缓存优化** | 元数据和路由建缓�?|
| **读写分离** | 利用副本实现读的水平扩展 |
| **网络优化** | 分片裁剪减少跨分片查�?|

---

## 📝 总结

**Distributed MiniSQL** 是一�?*完整的分布式数据库学习系�?*，涵盖：

�?**完整的SQL解析** - 从词法→语法→语义的编译器前�? 
�?**优化�?* - 经典的查询优化规则（谓词下推、列剪裁等）  
�?**分布式执�?* - 分片、副本、故障转移、负载均�? 
�?**高可�?* - 自动故障检测和恢复机制  
�?**易于学习** - 核心逻辑清晰，代码结构规�? 

特别适合用于**课程设计、实验展示和分布式数据库学习**�?
