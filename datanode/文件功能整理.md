# DataNode 文件功能整理

## 📁 文件结构

```
datanode/src/edu/minisql/datanode/
├── DataNodeServer.java        # 数据节点服务主类
├── DataNodeConfig.java        # 配置管理
├── JdbcSqlExecutor.java       # SQL执行�?
├── MasterClient.java          # Master通信客户�?
├── NodeStatus.java            # 节点状态枚�?
└── rpc/
    ├── RpcMessage.java        # RPC消息格式
    └── Json.java              # JSON序列化工�?
```

---

## 📋 文件详细说明

### 1. `DataNodeServer.java` �?核心服务�?
**位置**: `edu/minisql/datanode/DataNodeServer.java`

**功能**: 数据节点的主服务器，整个DataNode的启动入口和核心控制中心

**关键职责**:
- **HTTP服务启动** - 创建HttpServer监听本地端口(默认9101)，提供RPC服务
- **RPC请求处理** - 处理POST请求到`/rpc`路由，解析method和params
- **SQL执行代理** - 接收executeSql请求，委托给JdbcSqlExecutor执行
- **读写计数** - 统计读请求数(reads)和写请求�?writes)，用于负载均�?
- **心跳检�?* - �?秒向Master发送心跳，报告节点状态、读写计数、错误信�?
- **节点注册** - 启动时向Master注册自己的信�?
- **状态管�?* - 维护节点状�?STARTING→ONLINE→可能变为RECOVERING/OFFLINE)
- **错误追踪** - 记录最后一次错误信�?

**主要成员方法**:
```java
void start()                                    // 启动服务�?
void startHeartbeat()                          // 启动心跳线程
void handleRpc(HttpExchange exchange)          // RPC请求处理�?
Map<String, Object> executeSql(params)        // 执行SQL方法
```

**典型使用流程**:
```
1. DataNodeConfig 从命令行参数加载配置
2. DataNodeServer 创建并启�?
3. 向Master注册并开启HttpServer
4. 后台心跳线程定期上报状�?
5. 接收RPC请求并执行SQL
```

---

### 2. `DataNodeConfig.java` ⚙️ 配置�?
**位置**: `edu/minisql/datanode/DataNodeConfig.java`

**功能**: 数据节点的配置参数管理，使用Java record数据�?

**包含配置�?*:
| 配置�?| 类型 | 说明 | 默认�?|
|------|------|------|--------|
| `nodeId` | String | 节点ID | `dn1` |
| `host` | String | 监听地址 | `127.0.0.1` |
| `port` | int | 监听端口 | `9101` |
| `masterUrl` | String | Master服务地址 | `http://127.0.0.1:8080` |
| `databaseType` | String | 数据库类�?| `H2` |
| `jdbcUrl` | String | JDBC连接字符�?| `jdbc:h2:./data/dn1` |
| `jdbcUser` | String | 数据库用户名 | `sa` |
| `jdbcPassword` | String | 数据库密�?| 空字符串 |
| `jdbcDriver` | String | JDBC驱动类名 | 空字符串 |

**加载优先�?* (从高到低):
1. 命令行参�?`--key value`
2. 环境变量 (如`MINISQL_NODE_ID`)
3. 内置默认�?

**典型命令行启�?*:
```bash
java DataNodeServer --node dn2 --port 9102 --master http://coordinator:8080
```

**功能方法**:
```java
static DataNodeConfig fromArgs(String[] args)  // 从命令行参数解析配置
```

---

### 3. `JdbcSqlExecutor.java` 🔧 SQL执行引擎
**位置**: `edu/minisql/datanode/JdbcSqlExecutor.java`

**功能**: 通过JDBC连接到本地数据库并执行SQL语句

**核心职责**:
- **JDBC连接管理** - 为每个SQL语句创建新的JDBC连接(连接池级�?
- **SQL执行** - 使用Statement执行各类SQL(SELECT/INSERT/UPDATE/DELETE)
- **结果集处�?* - 将ResultSet转换为Map<String,Object>格式
- **异常处理** - 捕获SQLException并返回错误信�?
- **驱动加载** - 如配置了jdbcDriver则动态加载驱动类

**返回结果格式**:
```json
{
  "success": true/false,
  "columns": ["col1", "col2", ...],
  "rows": [[val1, val2, ...], ...],
  "affectedRows": 0,
  "error": "error message or null"
}
```

**功能方法**:
```java
Map<String,Object> execute(String statementText)  // 执行SQL语句
Map<String,Object> resultSet(ResultSet rs)      // 转换结果�?
```

**执行流程**:
```
1. 建立JDBC连接
2. 创建Statement
3. 执行SQL
4. 判断是否有结果集
   - �? 提取列名和行数据
   - �? 返回影响行数
5. 异常捕获并返回错误信�?
```

---

### 4. `MasterClient.java` 📞 Master通信客户�?
**位置**: `edu/minisql/datanode/MasterClient.java`

**功能**: DataNode与Master/Coordinator之间的通信代理

**核心职责**:
- **节点注册** - 启动时向Master注册本节点信�?
- **心跳上报** - 定期向Master发送心跳，报告状态和统计信息
- **RPC调用** - 通过HTTP POST发送JSON-RPC请求到Master

**发送心跳包�?*:
```json
{
  "nodeId": "dn1",
  "status": "ONLINE",      // 节点状�?
  "readRequests": 100,     // 读请求数
  "writeRequests": 50,     // 写请求数
  "lastError": null        // 最后一个错�?
}
```

**RPC协议**: 遵循JSON-RPC 2.0标准
```json
{
  "jsonrpc": "2.0",
  "method": "heartbeat",
  "params": {...},
  "id": "uuid"
}
```

**功能方法**:
```java
void register()                                          // 注册节点到Master
void heartbeat(NodeStatus status, long reads, writes)  // 发送心�?
void call(String method, Map<String, Object> params)  // 发送RPC调用
```

**超时设置**:
- 连接超时: 3�?
- 请求超时: 10�?

---

### 5. `NodeStatus.java` 🔴 节点状态枚�?
**位置**: `edu/minisql/datanode/NodeStatus.java`

**功能**: 定义DataNode的所有可能状�?

**状态类�?*:
| 状�?| 说明 |
|------|------|
| `STARTING` | 启动�?- 节点正在启动过程 |
| `ONLINE` | 在线 - 节点工作正常，可以处理请�?|
| `RECOVERING` | 恢复�?- 节点故障后从副本恢复数据�?|
| `OFFLINE` | 离线 - 节点故障或主动下�?|

**状态转移关�?*:
```
STARTING
   �?
ONLINE ←→ RECOVERING
   �?
OFFLINE
```

---

### 6. `rpc/RpcMessage.java` 📨 RPC消息格式
**位置**: `edu/minisql/datanode/rpc/RpcMessage.java`

**功能**: 定义JSON-RPC 2.0消息格式的构造方�?

**提供方法**:

#### `request()` - 构造RPC请求
```java
static Map<String,Object> request(String method, Map<String,Object> params, String id)
```
返回格式:
```json
{
  "jsonrpc": "2.0",
  "method": "methodName",
  "params": {...},
  "id": "requestId"
}
```

#### `success()` - 构造成功响�?
```java
static Map<String,Object> success(Object result, Object id)
```
返回格式:
```json
{
  "jsonrpc": "2.0",
  "result": {...},
  "id": "requestId"
}
```

#### `error()` - 构造错误响�?
```java
static Map<String,Object> error(int code, String message, Object id)
```
返回格式:
```json
{
  "jsonrpc": "2.0",
  "error": {
    "code": 500,
    "message": "Error message"
  },
  "id": "requestId"
}
```

**常用HTTP状态码**:
- `405` - 只支持POST请求
- `500` - 内部错误或异�?

---

### 7. `rpc/Json.java` 🔄 JSON序列化工�?
**位置**: `edu/minisql/datanode/rpc/Json.java`

**功能**: 纯Java实现的JSON解析和序列化，不依赖外部�?

**核心方法**:

#### `parse()` - 解析JSON字符�?
```java
static Object parse(String text)  // 返回Object
```

#### `parseObject()` - 解析JSON对象
```java
static Map<String,Object> parseObject(String text)  // 返回Map
```

#### `stringify()` - 对象转JSON字符�?
```java
static String stringify(Object value)
```
支持类型:
- `null` �?`"null"`
- `String` �?带转义的双引�?`"value"`
- `Number`/`Boolean` �?直接toString
- `Map` �?JSON对象 `{key:value,...}`
- `Iterable` �?JSON数组 `[item1,item2,...]`

**字符转义规则**:
- `"` �?`\"`
- `\` �?`\\`
- 换行 �?`\n`
- 回车 �?`\r`
- 制表 �?`\t`

**内置Parser�?*: 
- 完整的JSON语法解析�?
- 支持对象、数组、字符串、数字、布尔值、null

---

## 🔗 模块依赖关系

```
DataNodeServer (服务主类)
    ├─ 创建 �?DataNodeConfig (读取配置)
    ├─ 创建 �?JdbcSqlExecutor (执行SQL)
    ├─ 创建 �?MasterClient (与Master通信)
    ├─ 使用 �?RpcMessage (构造RPC消息)
    ├─ 使用 �?Json (序列化JSON)
    └─ 追踪 �?NodeStatus (记录状�?

MasterClient (与Master通信)
    ├─ 使用 �?RpcMessage (构造请�?
    └─ 使用 �?Json (序列化JSON)

JdbcSqlExecutor (执行SQL)
    └─ 配合 �?DataNodeConfig (获取JDBC参数)
```

---

## 🚀 启动流程

```
1. main() 方法启动
   �?
2. DataNodeConfig.fromArgs(args) 读取配置
   �?
3. new DataNodeServer(config) 创建服务
   �?
4. server.start()
   ├─ HttpServer.create() 启动HTTP服务
   ├─ createContext("/rpc") 注册RPC路由
   ├─ masterClient.register() 向Master注册
   ├─ status = ONLINE
   └─ startHeartbeat() 启动心跳线程
   �?
5. 监听端口，等待RPC请求
   ├─ 接收POST请求�?rpc
   ├─ handleRpc() 处理请求
   └─ executeSql() 执行SQL
```

---

## 💬 通信时序

### 启动�?
```
DataNode                           Master/Coordinator
  �?                                   �?
  ├─ POST /rpc (registerNode) ────────→│
  �?                                   �?
  �?                       (success) ←│
  �?
  └─ (�?�? ┌─ POST /rpc (heartbeat)──→│
           �?                (success)�?�?
           └─ 循环发�?..
```

### 处理查询
```
Coordinator                        DataNode
  �?                                  �?
  ├─ POST /rpc (executeSql) ────────→│
  �?                                  ├─ JDBC 查询本地数据
  �?                                  �?
  �?                       (results)←│
  �?
  (收集多个DataNode结果并合�?
```

---

## 📊 总结�?

| 文件 | 类型 | 主要职责 | 依赖 |
|------|------|---------|------|
| DataNodeServer | �?| 服务启动、RPC处理、心跳管�?| 其他所有模�?|
| DataNodeConfig | Record | 配置解析、参数管�?| �?|
| JdbcSqlExecutor | �?| SQL执行、结果转�?| DataNodeConfig |
| MasterClient | �?| 与Master通信、节点注�?| RpcMessage、Json |
| NodeStatus | 枚举 | 节点状态定�?| �?|
| RpcMessage | �?| JSON-RPC格式构�?| �?|
| Json | �?| JSON序列�?反序列化 | �?|

---

## 🎯 关键设计要点

�?**模块化设�?* - 不同功能独立分开(配置、执行、通信)  
�?**无外部依�?* - Json和RpcMessage纯Java实现  
�?**心跳机制** - 定期上报状态便于故障检�? 
�?**标准�?* - 使用JSON-RPC 2.0和standard HTTP  
�?**灵活配置** - 命令行、环境变量、默认值支�? 
�?**并发处理** - CachedThreadPool处理多个RPC请求  

