# Distributed_Database 文件功能整理

## 目录结构概览

```
Distributed_Database/
├── pom.xml
├── README.md
├── Dockerfile
├── docs/
├── tests/
└── src/
    ├── edu/minisql/app/
    ├── edu/minisql/cluster/
    ├── edu/minisql/catalog/
    ├── edu/minisql/datanode/
    ├── edu/minisql/rpc/
    ├── edu/minisql/sql/
    ├── edu/minisql/storage/
    ├── parser/lexer/
    ├── parser/parser/
    ├── parser/semantic/
    ├── physical/
    └── planer/
```

---

## 一、顶层文件说�?

- `pom.xml`：Maven 构建配置，指�?Java 17 编译目标和项目坐标�?
- `README.md`：项目说明、功能列表、运行方式、Docker 和测试用例说明�?
- `Dockerfile`：构�?Docker 镜像的指令，支持容器化运行�?
- `docs/`：设计与使用文档（如分布式集群演示步骤）�?
- `tests/`：验收测试脚本和测试用例说明�?

---

## 二、应用入�?

- `src/edu/minisql/app/App.java`
  - 交互式命令行入口，启动单进程“模拟分布式集群”系统�?
  - 读取用户 SQL，调�?`MiniSqlSystem` 执行并打印结果与耗时�?

---

## 三、集群与协调模块（edu.minisql.cluster�?

- `MiniSqlSystem.java`
  - 启动和引导演示集群，创建 Master、DataNode、MetaStore 等组件�?
  - 管理持久化路径、RPC 端口、执行入口�?
- `MasterRpcServer.java`
  - 提供 Master �?HTTP RPC 服务入口，接�?DataNode 注册和心跳�?
- `Coordinator.java`
  - 集群调度核心，处�?SQL 执行、SHOW/FAIL/RECOVER 命令、路由与结果合并�?
- `HealthMonitor.java`
  - 监控 DataNode 健康状态，维护节点可用性与监控数据�?
- `RemoteDataNodeClient.java`
  - 负责�?DataNode 发送远程执行请求并接收执行结果�?
- `Router.java`
  - 集群路由器，根据 `MetaStore` 信息选择目标 DataNode 和分片�?
- `RemoteSqlResult.java`
  - 封装远程执行结果，包含列信息、行数据、影响行数、是否成功�?

---

## 四、目录缓存与运行时管理（minisql.cluster.planner�?

- `SqlPlanningModule.java`
  - SQL 规划模块，从 `MetaStore` 和解析结果生成执行计划�?
- `RuntimeCatalog.java`
  - 运行时目录，暴露表结构、分片信息、可用节点信息以供规划器使用�?
- `PlannedSql.java`
  - 保存规划后的 SQL 请求列表，包�?`RemoteExecutionRequest` 等�?

---

## 五、元数据与目录模块（minisql.catalog�?

- `Column.java`
  - 表列定义，包含列名、数据类型、是否主键�?
- `MetaStore.java`
  - 集群元数据存储，管理表结构、分片布局、主从副本、路由版本�?
  - 支持创建/删除表、查询分片、主副本提升等�?
- `ShardPlacement.java`
  - 分片布置记录，含分片编号、主节点、备份节点列表�?
- `TableSchema.java`
  - 表模式定义，包含列列表、分片键、分片数量、副本数量、分片布置�?

---

## 六、DataNode 模拟模块（edu.minisql.datanode�?

- `DataNode.java`
  - 模拟一�?DataNode 节点，维护节点状态、在�?离线、故障恢复、读写计数�?
- `NodeStatus.java`
  - 节点状态枚举：`STARTING`、`ONLINE`、`RECOVERING`、`OFFLINE`�?

---

## 七、RPC 支持模块（edu.minisql.rpc�?

- `RpcMessage.java`
  - JSON-RPC 2.0 消息构造工具，包括请求、成功响应、错误响应构建�?
- `Json.java`
  - �?Java JSON 序列�?反序列化实现，用�?RPC 请求/响应体处理�?

---

## 八、SQL 命令与解析模块（minisql.sql�?

- `SqlCommand.java`
  - 抽象 SQL 命令接口或基类，各类命令实现该接口�?
- `SqlParser.java`
  - 轻量级正则解析器，识�?`CREATE/DROP/INSERT/SELECT/DELETE/UPDATE` �?SQL 命令�?
  - 解析分片语法、JOIN 语法、FAIL/RECOVER 命令�?
- `QueryResult.java`
  - 封装 SQL 执行结果的统一结构，包括消息、列、行、影响数�?

### 各类命令实现
- `CreateTableCommand.java`：解析并表示 `CREATE TABLE ... SHARD BY HASH(...) SHARDS ... REPLICAS ...`�?
- `DropTableCommand.java`：表�?`DROP TABLE`�?
- `InsertCommand.java`：表�?`INSERT INTO ... VALUES(...)`�?
- `DeleteCommand.java`：表�?`DELETE FROM ... WHERE ...`�?
- `UpdateCommand.java`：表�?`UPDATE ... SET ... WHERE ...`�?
- `SelectCommand.java`：表示简�?`SELECT ... FROM ... WHERE ...` 查询�?
- `JoinCommand.java`：表示等�?`JOIN` 查询�?
- `ShowShardsCommand.java`：表�?`SHOW SHARDS`�?
- `ShowNodesCommand.java`：表�?`SHOW NODES`�?
- `ShowClusterCommand.java`：表�?`SHOW CLUSTER`�?
- `FailNodeCommand.java`：表�?`FAIL NODE ...`�?
- `RecoverNodeCommand.java`：表�?`RECOVER NODE ...`�?

---

## 九、本地引擎模块（minisql.storage�?

- `LocalMiniSqlEngine.java`
  - 本地 MiniSQL 引擎实现，负责对单节�?SQL 的本地执行、存储与查询�?

---

## 十、解析器模块（parser�?

### 1. 词法分析（parser/lexer�?
- `Lexer.java`
  - 基于字符流的词法分析器，�?SQL 文本分解�?Token�?
- `Token.java`
  - Token 结构定义，存储词法单元类型和值�?
- `tokenType.java`
  - Token 类型枚举，定义关键字、标识符、符号、字面量等�?

### 2. 语法树节点（parser/parser�?
- `ASTNode.java`：所�?AST 节点的基类或接口�?
- `ASTPrinter.java`：将 AST 转换成文本形式，便于调试�?
- `Parser.java`：主解析器，用递归下降或手工分析构�?AST�?

#### 表达式与语句节点
- `CreateTableStatement.java`：CREATE TABLE AST 节点�?
- `DropTableStatement.java`：DROP TABLE AST 节点�?
- `InsertStatement.java`：INSERT AST 节点�?
- `DeleteStatement.java`：DELETE AST 节点�?
- `UpdateStatement.java`：UPDATE AST 节点�?
- `SelectStatement.java`：SELECT AST 节点�?
- `AlterTableStatement.java`：ALTER TABLE AST 节点�?
- `TruncateTableStatement.java`：TRUNCATE TABLE AST 节点�?
- `TableReference.java`：表引用表示�?
- `JoinClause.java`：JOIN 子句表示�?
- `GroupByClause.java`：GROUP BY 表示�?
- `HavingClause.java`：HAVING 表示�?
- `OrderByClause.java`：ORDER BY 表示�?
- `LimitClause.java`：LIMIT 表示�?
- `WhereClause.java`：WHERE 条件表示�?

#### 表达式节�?
- `Expression.java`：表达式基类�?
- `BinaryExpression.java`：二元表达式（如比较、算术运算）�?
- `UnaryExpression.java`：一元表达式�?
- `ColumnExpression.java`：列引用表达式�?
- `LiteralExpression.java`：字面量值表达式�?
- `IdentifierExpression.java`：标识符表达式�?
- `FunctionCallExpression.java`：函数调用表达式�?
- `AggregateExpression.java`：聚合函数表达式�?
- `SubqueryExpression.java`：子查询表达式�?
- `Condition.java`：条件表达式组合�?
- `Assignment.java`：UPDATE 赋值项�?
- `ColumnDefinition.java`：列定义�?
- `Constraint.java`：约束定义�?
- `ConstraintType.java`：约束类型枚举�?
- `PrimaryKey.java`：主键定义�?
- `ForeignKey.java`：外键定义�?
- `Index.java`：索引定义�?
- `DataType.java`：数据类型定义�?
- `JoinType.java`：JOIN 类型枚举�?
- `OrderByItem.java`：排序项�?
- `Order.java`：排序方向枚举�?

### 3. 语义分析（parser/semantic�?
- `SemanticAnalyzer.java`
  - �?AST 执行语义检查，确保 SQL 语义正确�?
- `SemanticException.java`
  - 语义分析错误类型定义�?
- `SchemaCatalog.java`
  - 表模式目录，管理用户定义的表和列�?
- `TableSchema.java`
  - 表结构元数据（列、主键、约束等）�?
- `ColumnSchema.java`
  - 列元数据定义�?

---

## 十一、逻辑优化与规划模块（planer�?

- `LogicalPlan.java`
  - 逻辑执行计划表示，抽象查询计划节点�?
- `LogicalPlanBuilder.java`
  - �?AST 构建逻辑计划�?
- `LogicalPlanPrinter.java`
  - 打印逻辑计划结构，辅助调试�?
- `PredicatePushdown.java`
  - 谓词下推优化，将过滤条件尽量下推到数据源�?
- `ColumnPruning.java`
  - 列剪裁优化，仅保留查询需要的列�?
- `ConstantFolding.java`
  - 常数折叠优化，在规划阶段计算常量表达式�?
- `JoinReordering.java`
  - JOIN 重排序优化，调整多表连接顺序�?

---

## 十二、物理执行与分布式模块（physical�?

- `PhysicalPlan.java`
  - 物理执行计划表示�?
- `PhysicalPlanBuilder.java`
  - 将逻辑计划转换成物理执行计划�?
- `PhysicalPlanPrinter.java`
  - 打印物理计划结构，辅助调试�?
- `RemoteStatement.java`
  - 远程执行 SQL 语句封装�?
- `RemoteStatementGenerator.java`
  - 根据逻辑/物理计划生成发送给 DataNode �?SQL�?
- `RemoteExecutionRequest.java`
  - 表示对某�?DataNode 的远程执行请求�?
- `RemoteExecutionRequestBuilder.java`
  - 构建 `RemoteExecutionRequest`�?
- `ClusterMetadata.java`
  - 集群元数据表达�?
- `DataNodeMetadata.java`
  - 单个 DataNode 元数据信息�?
- `TableMetadata.java`
  - 表元数据信息描述�?
- `ShardMetadata.java`
  - 分片元数据描述�?
- `DatabaseType.java`
  - 数据库类型枚举或标识�?

---

## 十三、补充说�?

### 1. `Distributed_Database` 的核心职�?
- 提供一个“单进程模拟分布式集群”的 MiniSQL 原型系统�?
- 支持分片、主从副本、故障模拟、恢复、读负载均衡和跨分片查询�?
- 通过 `SqlParser` 解析输入 SQL，通过 `Coordinator` 调度执行，通过 `MetaStore` 管理路由信息�?

### 2. 典型流程
- 用户�?`App` 中输�?SQL�?
- `MiniSqlSystem` 启动集群并加载持久化状态�?
- `SqlParser` �?SQL 转成命令对象�?
- `Coordinator` �?`SqlPlanningModule` 生成执行计划�?
- `RemoteDataNodeClient` �?DataNode 发送执行请求�?
- 执行结果返回并由 `Coordinator` 合并�?

---

## 十四、建议阅读顺�?

1. `README.md`：项目简介与运行方式�?
2. `App.java`：启动流程与交互�?
3. `MiniSqlSystem.java` + `Coordinator.java`：集群调度核心�?
4. `MetaStore.java`：分片与副本管理逻辑�?
5. `SqlParser.java`：SQL 解析入口�?
6. `LogicalPlanBuilder.java` / `SqlPlanningModule.java`：查询规划与优化�?
7. `RemoteDataNodeClient.java`：远程执行与结果合并�?
